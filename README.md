Run Backend:

```shell
clj -M:backend
```

Run Frontend:

```shell
yarn start
```

Run Tests:

```shell
clj -M:test
```

Formatter:

```shell
clj -M:format-check
```

Run Linter:

```shell
clj -M:kondo
```